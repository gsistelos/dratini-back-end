meta {
  name: create_blocked
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: none
}

body:json {
  {
    "username": "blocked",
    "email": "blocked@gmail.com",
    "password": "blocked123"
  }
}

assert {
  res.status: eq 200
}

script:post-response {
  const data = res.getBody()
  
  bru.setEnvVar("blockedId", data.id)
}
