meta {
  name: get_blocked_by
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/users/{{blockedId}}/blocked-by
  body: none
  auth: bearer
}

auth:bearer {
  token: {{blockedToken}}
}

assert {
  res.status: eq 200
  res.body: isArray
  res.body.length: gt 0
}
