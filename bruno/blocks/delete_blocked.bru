meta {
  name: delete_blocked
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/users/{{blockedId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{blockedToken}}
}

assert {
  res.status: eq 200
}
