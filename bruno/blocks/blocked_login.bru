meta {
  name: blocked_login
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: none
}

body:json {
  {
    "email": "blocked@gmail.com",
    "password": "blocked123"
  }
}

assert {
  res.status: eq 200
  res.body.token: isDefined
}

script:post-response {
  const data = res.getBody()
  
  bru.setEnvVar("blockedToken", data.token)
}
