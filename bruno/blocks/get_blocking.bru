meta {
  name: get_blocking
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/users/{{blockerId}}/blocking
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isArray
  res.body.length: gt 0
}
